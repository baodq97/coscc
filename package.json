{
  "name": "cos-baodo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:node && npm run test:python",
    "test:node": "node --test 'channel/*.test.mjs' '.claude/scripts/*.test.mjs'",
    "test:python": "uv run python -m unittest discover -s app -p '*_test.py' -t ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
