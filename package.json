{
  "name": "coscc",
  "version": "0.4.6",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:node && npm run test:python",
    "test:node": "node --test '.claude/scripts/*.test.mjs'",
    "test:python": "uv run python -m unittest discover -s coscc -p '*_test.py' -t ."
  }
}
