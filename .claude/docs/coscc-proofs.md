# The proofs, and what each one costs

Read this before running `npm run e2e`, a script under `scripts/` or `scripts/capture_screens.py`, and before writing a proof. Moved here whole from `.claude/rules/coscc-app.md` (`0094`); the history ("Since `00xx`") is kept at this tier.

Since `0095` a claim worth keeping is a test in `npm test` or a case in `npm run e2e`; a unit does not write a proof script of its own (`.claude/skills/write-plan/SKILL.md`, invariant 4). The `scripts/verify_*.py` left are the measuring tools of units whose outcome is still being measured. Each is run at a terminal on the date its row gives, and can go once its unit's outcome is recorded.

- **`npm run e2e` is `scripts/e2e.py`.** Since `0095`. Browser, no session, no quota, no
  network: it starts `coscc.run` on `COS_HOST`/`COS_PORT` with a temporary working folder
  and data root, two workspaces cloned from bare-directory remotes, and a password and one
  session written in before it starts, so every case runs past the `0070` login. It needs
  the port free and a bundle built for it (`COS_HOST=127.0.0.1 COS_PORT=<port> uv run
  coscc-build`). Each case is a function named for what it shows. It is not part of `npm
  test` and does not run in CI.
- **`scripts/capture_screens.py` overwrites `<repo>/.web`, then builds it back.** Since
  `0083`. `coscc.run` always serves `<repo>/.web`, so the bundle it builds for its own port
  (18783) replaces the checkout's (`.cos/0083_*/spike.md ## U2`). When the old one was
  current for this environment's `COS_HOST`/`COS_PORT`, it is rebuilt at the end and the
  last line says so; interrupted, or a failed rebuild, leaves every proof that needs the
  default bundle at exit 2 until `uv run coscc-build`. Its screenshots are what `review`
  looks at, as an agent reading PNGs, not a person; the `ship` gate reads only the words of
  `review.md ### Screens`, never the images (`.claude/CLAUDE.md`, *A screenshot is not a
  person's look*).

| | |
|---|---|
| `verify_0037.py` | plain: no session, no quota, no network; temporary data root, only the SDK client replaced. Claim (b) calls the SDK's private `SubprocessCLITransport._build_command`; if that cannot be called it is exit 2, not a pass. `--baseline` and `--measure` read `<COS_DATA_DIR>/cos.db` (`mode=ro`, never through `Data`) and `~/.claude/projects/*/<session>.jsonl`, and write only to `<COS_DATA_DIR>/measurements/`; too few sessions to compare is exit 2. `--paid` **spends real money**: six `claude -p` runs, three per branch. No `claude` on `PATH` is exit 2 |
| `verify_0041.py` | plain: no session, no quota, no network; temporary data root and a fake `gh` first on `PATH`. Needs `git` and `uv`; either missing is exit 2. `--measure` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and the store's `pr.md` files, and writes only to `<COS_DATA_DIR>/measurements/`; fewer than five `pr` steps since `0041` is exit 2. `--paid` **spends real money, pushes to `main` of `COS_PROOF_REPO` and leaves two pull requests open there**: two real `pr` steps; unset is exit 2. Not run when it was written |
| `verify_0044.py` | plain: no session, no quota, no network; temporary data root, the app driven in-process over ASGI, every session a stand-in and `Sessions.stream` itself replaced by a counter it prints. Needs `node`; missing is exit 2. R14 is `cos.test.mjs`'s own test, run by name. `--measure --cwd <workspace>` **spends real money**: up to 8 sessions at the `precedent` grant's $1.00 ceiling (chosen, not measured), one per unit of `spec.md` R16, on this machine's login; it writes no artifact and no run-log row — Activity and Cost do not see that spend — and only `<COS_DATA_DIR>/measurements/0044-<date>.json` and `.md`. Run it at a terminal (`0076`). It measures only the share answered; whether each answer is right is Leif's and the originator's grading of the `.md` |
| `verify_0053.py` | browser, no session, no quota, no network; needs `COS_PORT` free and a bundle built for it (`COS_HOST=127.0.0.1 COS_PORT=18753 uv run coscc-build`), a temporary data root, a fake `gh` first on `PATH` and `CLAUDE_CONFIG_DIR` in the temporary folder. Counts the socket's received bytes in chromium (CDP) for a load of 42 and of 200 cards and for a change of workspace; the fixture is this checkout's fourteen `.cos/` units repeated under `1000+` numbers. (f) R3 and (i) C4 drive `StudioState` in-process with the session replaced and count re-serialized deltas, not frames. `--url <base> --ws <name> --from <name>` measures a running app with `COS_PROOF_PASSWORD`, writes nothing and has no (f): run it at a terminal, since a step sees only its scratch data root (`0076`). Does not measure the intent's outcome — the real workspace on the installed app by 2026-10-16 |
| `verify_0060.py` | plain: no session, no quota, no network; temporary directory with a fixture run log and fixture transcripts. Needs `git` (it loads `coscc/policy.py` from `01699b8` with `git show`) and `bash` (`type -t`); either missing, or that commit absent from a shallow clone, is exit 2. `--measure --since --until [--confirmed FILE]` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and `COS_TRANSCRIPTS_DIR`, and writes only to `<COS_DATA_DIR>/measurements/`; no session in the window is exit 2. It does not import `coscc`. "Fake" depends on the `PATH` of the machine running it |
| `verify_0061.py` | plain: no session, no quota, no network; temporary directory. Needs `node` and `git` (it loads `cos.mjs` from `git merge-base HEAD origin/main`) and `uv`; a missing one, or no merge-base, is exit 2. `--root <dir>` (repeatable) adds a store such as `~/.cos/units/<slot>` to the comparison. `--measure` reads every `<COS_DATA_DIR>/units/*/.cos/` through `cos.mjs status --json` and writes only to `<COS_DATA_DIR>/measurements/`; no merge line yet (this unit's `ship.md`) or fewer than 5 units shipped after it is exit 2. Kind (b) of the intent's wasted round is a person reading pull request history; it does not conclude it |
| `verify_0074.py` | plain: no session, no quota, no network; temporary data root, the session replaced, the app driven in-process over ASGI. Needs `node`; missing is exit 2. R15 runs `cos.mjs gate` for every stage and `next` for every fixture unit before and after the three kinds of record. `--measure` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and this unit's `ship.md` under `<COS_DATA_DIR>/units/*/.cos/`, and writes only to `<COS_DATA_DIR>/measurements/`; no merge line, or the 14-day window still open, is exit 2. Run it at a terminal: inside a step it reads a scratch data root (`0076`). It does not print the `lệch` count of each shortlist in use: that needs the board as it stood then |
| `capture_screens.py` | not a proof: the screenshots a UI unit's `impl` takes (`0083`). Browser, port 18783 on `127.0.0.1` (chosen) must be free; no session, no quota, no network; temporary data root, bare-directory remote, a fake `gh`, four fixture units — since `0082` `0004_finished` has a passed deadline and two open questions — and one chat conversation written into a temporary `CLAUDE_CONFIG_DIR`. Writes each page's visible text beside its PNG as `.txt`. Builds a bundle for its port when none is current and rebuilds the checkout's afterwards, about 26 s more (`.cos/0083_*/spike.md ## U2`); the whole run measured 50.5 s there. Writes only `.screens/` |
| `verify_0080.py` | plain: no session, no quota, no network; temporary stores and data roots, a stand-in session driven through `Service.run_step`. Needs `node`, `git` and a merge-base between `HEAD` and `origin/main`; any missing is exit 2. `--measure` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and this unit's `ship.md`, writes only to `<COS_DATA_DIR>/measurements/`, and does not import `coscc`: **run it at a terminal** (`0076`). It counts `end` rows of `spike` by `spike_md` up to 2026-11-01 +07:00 (the zone chosen, not sourced). Does not measure whether a real session keeps its progress file, a step cut by the app going down, or a spike run at a terminal |
| `verify_0085.py` | plain: no session, no quota, no network; temporary stores and data roots, `cos.mjs` on fixture units, and a stand-in session driven through `Service.run_step` with the gate stood in for. Needs `node` and `git`; either missing is exit 2. `--measure` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`), this unit's `ship.md` and each unit's `review.md` under `<COS_DATA_DIR>/units/`, writes only to `<COS_DATA_DIR>/measurements/`, and does not import `coscc`: **run it at a terminal** (`0076`). It judges the first 10 `review` steps after the merge by `review_md`, and exits 2 until there are 10, with no deadline (`0085` `spec.md ## Answers, câu 2`). Does not measure a real closing turn's cost after a 40-turn session, a review run at a terminal, or a step cut by the app going down |
| `verify_0083.py` | plain: no session, no quota, no network; temporary directory, a git repository with a bare-directory remote, a fake `gh` and a `git` shim that logs argv, both first on `PATH`. Needs `node`, `git`, `uv` and a merge-base with `origin/main` (R12 runs the `cos.mjs` there); any missing is exit 2. Claim (g) imports `capture_screens.scan`. `--measure [--today YYYY-MM-DD]` reads this checkout's `origin/main` and, through `cos.mjs status --json`, the one `<COS_DATA_DIR>/units/<slot>/.cos/` holding this unit's `ship.md` (every workspace numbers from `0001`), and writes only to `<COS_DATA_DIR>/measurements/`; no merge line yet or before 2026-12-01 is exit 2. Run it at a terminal (`0076`) |
| `verify_0088.py` | plain: two real CLI sessions and nothing billed — their `ANTHROPIC_BASE_URL` is a stand-in on `127.0.0.1` that counts requests, looks for one word in each body in memory, answers 400 and writes nothing, since the CLI sends this machine's token to it; each carries a `CLAUDE.md` of about 350 KB, past the one-argument limit; otherwise only `ClaudeSDKClient` in `coscc.sessions` replaced, temporary data root. Claims (a) and (b) call the SDK's private `_build_command`, (d) needs `git` and a merge-base with `origin/main`; either failing, or no bundled CLI, is exit 2. Hashes `~/.claude/settings*.json`, `installed_plugins.json` and the names under `~/.claude/skills/` before and after, and writes none of them. `--paid` **spends real money**: four sessions on this machine's login at the ids `coscc/models.json` ships, one at `cwd=$HOME` holding `Bash` under a gate that refuses every call; exit 2 without a `Bash(...)` rule in `~/.claude/settings.json` whose prefix it knows to be read-only. `--measure` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) — `runs` and `step_events` — and this unit's `ship.md`, and writes only to `<COS_DATA_DIR>/measurements/`: **run it at a terminal** (`0076`), after 2026-10-16 (+07:00, chosen) and before `step_events` purges the window's first steps (`KEEP_DAYS` = 30 after each) |
| `verify_0091.py` | plain: no session, no quota, no network; a fake CLI in a temporary directory that prints one JSON line and exits, read by the SDK's private `SubprocessCLITransport` with the options `sessions._options` builds. It pushes one line of 32 MiB through a pipe. An SDK that renames that class, or a `connect()` that fails, is exit 2. `--measure` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) — `runs`, `step_runs` and `step_events` — and this unit's `ship.md`, writes only to `<COS_DATA_DIR>/measurements/`, and does not import `coscc`: **run it at a terminal** (`0076`), up to 2026-10-16 (+07:00, chosen) and before `step_events` of the steps it counts are purged (`KEEP_DAYS` = 30 after each). Does not measure how long a real CLI's line is for a given image — only a lower bound on the ratio is known (`.cos/0091_*/spike.md ## U2`) — nor whether the model saw the image |
| `verify_0111.py` | browser, no session, no quota; needs `node`, `git`, `uv`, Playwright's chromium and 18783 free, and uv's and npm's caches warm (cold, it needs the network — exit 2, not 1); any missing is exit 2. Clones this checkout into a temporary directory with a bare-directory remote, takes `/board` there with `capture_screens.py`, and drives `Service.run_step(..., "review")` in-process with the worktree, gate and session stood in for, through (a) a rebase that touches no screen, (b) one that does, over a stale `.web`, and (c) one with 18783 held — four captures, a few minutes. `--measure --workspace <journal key> [--since] [--until]` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and each unit's `review.md`, writes nothing, and does not import `coscc`: it prints the open `high` findings of each round that asked for changes after a pushed integration, 2026-09-26 to 2026-10-16 by default, for a person to count those only about a stale manifest. Run it at a terminal (`0076`) |
| `verify_0043.py` | no session, no quota, no network, writes nothing. Reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and hands one workspace's records to `coscc.autopilot.measure`: exit 0 once 3 units reached the stop before `ship` with no person's `start` outside a stop, 1 fewer, 2 no `cos.db`. Measures the intent, not the plan: run it after 2026-10-31, at a terminal on the machine the app runs on (`0076`) |
| `verify_0104.py` | no session, no quota, no network, writes nothing. Reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and hands one workspace's records to `coscc.autopilot.measure_order`, from its first `autopilot-pick` to the end of `--until` (default 2026-10-02, the machine's day): exit 0 once the autopilot started ≥ 10 steps with no V1–V3 against the shortlist each pick recorded, 1 otherwise, 2 no `cos.db` or a bad `--until`. The reasons it checks are the app's own, so it proves consistency, not that a reason was true. Measures the intent, not the plan: run it after 2026-10-02, at a terminal on the machine the app runs on (`0076`) |
| `verify_0106.py` | no session, no quota, no network, writes nothing. Reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and hands one workspace's records to `coscc.autopilot.measure_reruns`, over `--since` (default the day of the first `answer` record) to `--until` (default 2026-10-31), the machine's days: each `answer` record that finished a shortlisted `draft`'s questions with the autopilot on is one case, classed `on-time` (a pick or start within 300 s, or 600 s after a `full` stop), `held`, `reruns`, `gate`, or missed — `late`, `cap`, `stop:<kind>`, `none`. Exit 0 no case missed, 1 one did, 2 no `cos.db` or a bad argument, 3 no case to measure. 300 s stands in for the next pass (spec C3) and a hold placed after the answer reads `none` (C7): read each missed line, not only the code. Measures the intent, not the plan: run it after 2026-10-31, at a terminal on the machine the app runs on (`0076`) |
| `verify_0105.py` | no session, no quota, no network, writes nothing. Reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) — every workspace's `runs`, since the cap is the app's, and `prefs.autopilot_daily_cap_usd` — and hands them to `coscc.autopilot.measure_days`, whose spend is the cap's own `spent_on`: exit 0 once one day had ≥ 5 integrations and a failed step in the workspace, an autopilot start there, and a spend within the cap, 1 none, 2 no `cos.db` or a bad argument. The cap is today's, not that day's; days are the machine's, each printed with its UTC bounds. Measures the intent, not the plan: run it after 2026-09-30, at a terminal on the machine the app runs on (`0076`) |
| `verify_0093.py` | no session, no quota, no network, writes nothing. Reads `<COS_DATA_DIR>/cos.db` (`mode=ro`, never through `Data`) and imports only `coscc.spend`; compares the *Cost* model with the spec's reference queries for every unit, stage, local day and unit by stage, in this machine's zone. No `cos.db`, no `end` row, or an `end` row that is not JSON is exit 2. **Run it at a terminal** on the machine the app runs on (`0076`). Does not compare against Leif's own total, whose script was never committed |
| `verify_0094.py` | plain: no session, no quota, no network; a temporary `cos.db` written through `Journal`, and the unit tests of R6–R7, R10–R11 and R13–R16 in child processes. `--measure --workspace <name> --min-version X.Y.Z --before-since <ISO> [--repo <dir>]` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`) and this unit's `ship.md`, writes only to `<COS_DATA_DIR>/measurements/`, and does not import `coscc`: **run it at a terminal** (`0076`), on or after 2026-10-16. Fewer than 10 steps before or after in any of `pr`, `ship`, `impl`, `review` is exit 2. It does not measure whether an agent read what it was pointed to |
| `verify_0110.py` | plain: no session, no quota, no network; a temporary `cos.db` written through `Journal` with its clock patched, and `--measure` run on it. `--measure --workspace <name> --window baseline\|result` reads `<COS_DATA_DIR>/cos.db` (`mode=ro`), writes nothing, and does not import `coscc`: **run it at a terminal** (`0076`). `baseline` exits 0 only on R2's exact numbers; `result` is exit 2 until `0110` has shipped and while fewer than 10 units qualify, and is the intent's answer only after 2026-10-16. It does not measure whether an `impl` read the section, nor why a unit cost what it did |

Exit codes: `0` pass, `1` the page is broken, `2` the environment is not ready.

`npm run e2e`, `verify_0053` and `capture_screens.py` open a real browser. **In a
checkout** the bundle hardcodes its own address, so none can move to a spare port without a
bundle built for it: stop the app first, or build for another port, and never run two of
them at the same time. A wheel installed by `install.sh` behaves the other
way — `coscc/frontend.py` rewrites the address at startup, because a packaged install has
no Node to rebuild with. Both sentences are true; which one applies depends on which of the
two shapes you are looking at, and `coscc/run.py` is where they part.

A figure carries across a rewrite only if the mechanism did not change. `0006 spec.md` C2
says this about swapping the store; it holds the same way for swapping the interpreter.
